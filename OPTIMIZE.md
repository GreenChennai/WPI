# WPI v3.0.2 优化清单

> 目标：① 修复静态导出（PNG）下方折叠区 canvas 动画「噪波墨流消失 / 入水扩散墨滴看情况」；② 修复 GIF/MP4 中「噪波墨流」「入水扩散」「文字裂解构」动画一闪一闪
> 文件：src/core/、OPTIMIZE.md
> 关联：V3.0.1 用「33ms 节流 + 整页单拍」虽消除卡死，但整页单拍（captureBeyondViewport）对视口外的 2D canvas 动画会取到陈旧光栅

---

## 优化项列表

### 1. 修复静态导出下方折叠区 canvas 陈旧光栅（噪波墨流消失 / 墨滴看情况）
- **现状**：INKFLOW 800px PNG 中「噪波墨流」流场几乎看不见（canvas 被淡入雾底糊成一片浅色块）、「入水扩散」墨滴时有时无。
- **根因**（已复现）：整页单拍 `captureBeyondViewport` 对视口外的 2D canvas 不刷新光栅——canvas 的 JS 位图在更新（每 33ms 一帧），但合成器只在它处于屏内时才提交纹理，整页单拍取到的是陈旧/欠渲染的一帧（实测下方折叠流场暗像素 ~1132 vs 在屏 ~3000-5100）。墨滴截到的是随机中间态 → 看情况。
- **修复**：静态导出在页面存在「视口外 canvas」时改用分块滚动截取（`capture_highres` 现有路径，`force_tiled=True`）：滚动到每段使其在屏、等 400ms + 2 rAF 帧再截取视口 clip 后拼接 → 每个 canvas 都以在屏状态截取。新增 `CaptureEngine.has_below_fold_canvas()` 判定；无此情况仍单拍优先。
- **验证**：INKFLOW 800px PNG 流场暗像素 1132→2594（在屏 ~3000-5100），墨滴始终为当前在屏状态（不再陈旧空白）；INK 800x7577 PNG 37s 完成。
- **状态**：✅ 已完成

### 2. 修复 GIF/MP4 下方折叠区 canvas 动画一闪一闪
- **现状**：整页 GIF/MP4 中「噪波墨流」「入水扩散」「文字裂解构」三个 canvas 一闪一闪，顶部英雄区等正常。
- **根因**（已复现）：同上——整页逐帧单拍对视口外 canvas 只偶尔刷到新光栅（陈旧帧与在屏帧随机交替）→ 闪烁；顶部英雄区在屏所以正常。加快节流（8-16ms）只减闪烁仍欠渲染，无法根治。
- **修复**：GIF/MP4 改为**滚动逐帧录制**（`capture_scroll_frames`）：视口内截取（逐帧 CDP JPEG 加速），每段内容在屏时截取 → 所有 canvas 动画流畅无闪烁，并自然触发 reveal-on-scroll 入场动画、覆盖整页。首帧停顶部捕获首屏，随后在 max_wait 内均匀滚到底。高度锁定用 `scroll_limit` 限制录制范围。rAF 节流保持 33ms（INK 页不再卡死）。
- **验证**：INKFLOW 滚动帧 60 帧 800x800，流场区在屏时逐帧变化（9/9），不再冻结/交替；INK GIF 22.6s 完成不卡死；demo PNG/GIF/PDF 回归通过。
- **状态**：✅ 已完成

### 3. 版本号升级到 3.0.2
- **目标**：`src/config/presets.py`、`pyproject.toml`、`tools/build.py`、workflow 版本段统一改为 3.0.2。
- **状态**：✅ 已完成

---

## 依赖关系

- 项 1、2 共享同一根因（整页单拍对视口外 canvas 用陈旧光栅），分别落在静态（分块截取）与动画（滚动录制）两条路径。
- 项 3 依赖 1-2 完成后统一改版号。