# WPI v3.0.4 优化清单

> 目标：GIF/MP4 整页动画进一步**流畅化**——① 噪波墨流（噪波墨流）拖尾线在所有帧都正常（V3.0.3 只有首帧有拖尾）；② 入水扩散不再频闪（水墨晕开忽明忽暗一帧）；③ 渲染节流更宽容（33ms → 16ms），动画更顺滑。
> 文件：src/core/capture_engine.py、src/config/presets.py、OPTIMIZE.md
> 关联：V3.0.3 用「整页截图 + 视口外 canvas 实时位图合成」恢复整页同时动画，但视口外 canvas 多为透明背景（噪波墨流 / 入水扩散只绘制粒子与墨滴），直接把实时位图粘贴到整页截图上时，透明区域透出底下的**陈旧合成纹理** → 表现为墨流拖尾只在首帧出现、入水扩散忽明忽暗闪烁。

---

## 优化项列表

### 1. 修复视口外透明 canvas 的陈旧纹理透出（墨流拖尾 / 入水扩散频闪）
- **现状**：V3.0.3 整页合成后，噪波墨流只在 GIF 首帧有拖尾线、后续帧消失；入水扩散（水墨晕开）播放时偶发「突然变深一帧、再恢复好几帧」的频闪。
- **根因**（已复现并量化）：噪波墨流 / 入水扩散 canvas 是**透明背景**（只绘制粒子与墨滴，其余透明）。整页单拍对视口外 canvas 取到的是陈旧合成纹理；把这些 canvas 的实时位图（`toDataURL`，含大量透明像素）用 `paste(img, mask=img)` 粘贴到截图上时，透明像素不覆盖截图 → 截图里**冻结的旧纹理透出**。墨流拖尾是逐帧累积的半透明结构，旧纹理（某一冻结态）盖住后续实时拖尾 → 视觉上「只有首帧有拖尾」；入水扩散在墨滴淡出（alpha 低）的帧，透明区域透出冻结的旧墨滴 → 「忽明忽暗一帧」。
- **修复**：在 `_composited_shot` 里整页截图**前**，先用 `_set_below_fold_visibility(True)` 把视口外 canvas 设为 `visibility:hidden`（保留布局、不阻断 rAF 与画布绘制，故 `toDataURL` 仍可取实时位图），等合成器按隐藏态重绘一帧后再截图——此时 canvas 区域只显示底层页面（section 背景），不再含陈旧纹理；随后把实时位图合成上去，透明区域正确透出页面背景而非旧纹理。截图后恢复 `visibility:''`。`visibility:hidden` 不清除画布位图，实时位图与页面背景严格对齐。
- **验证**：INKFLOW 800px 整页 GIF 40 帧，噪波墨流区域「拖尾感」像素在 **39/39 帧**均非零（首帧不再孤立偏高）；入水扩散区域暗像素逐帧**平滑过渡**（无突发深/浅异常帧）；INK 800x7577 整页 GIF 不卡死。pytest 46/46。
- **状态**：✅ 已完成

### 2. 渲染节流更宽容（33ms → 16ms），动画更顺滑
- **现状**：V3.0.3 用 33ms（~30fps）节流，用户反馈「动画还是不够流畅」，希望更宽容。
- **修复**：`RENDER_RAF_THROTTLE_MS` 由 33 改为 16（~60fps）。把页面 `requestAnimationFrame` 替换为 `setTimeout(cb, 16)`，仍远低于「数百 fps 占满主线程」的卡死阈值，重交互页（INK）导出正常（不卡死，仅因整页截图/settle 较重而导出偏慢，用户已接受更长导出时间）。
- **验证**：INKFLOW 整页 GIF/MP4 动画明显更顺；INK 导出不卡死（~65s 完成，早期停止于静态内容）。
- **状态**：✅ 已完成

### 3. 版本号升级到 3.0.4
- **目标**：`src/config/presets.py`、`pyproject.toml`、`tools/build.py`、workflow 版本段统一改为 3.0.4。
- **状态**：✅ 已完成

---

## 依赖关系

- 项 1 在 V3.0.3「整页截图 + 视口外 canvas 位图合成」基础上，针对透明背景 canvas 增加「截图前隐藏视口外 canvas」一步，消除陈旧纹理透出。
- 项 2 独立调整全局节流间隔，配合项 1 进一步提升流畅度。
- 项 3 依赖项 1-2 完成后统一改版号。
